# 🤖 VoiceBot - مشروع روبوت الدردشة الصوتي المتكامل (End-to-End Voice Chatbot)

هذا المشروع يهدف إلى بناء روبوت دردشة صوتي متكامل يعمل بالذكاء الاصطناعي، يجمع بين ثلاثة مكونات رئيسية: التعرف على الصوت، توليد الردود باستخدام نموذج لغوي ضخم (LLM)، وتحويل النص إلى كلام مسموع.

## 📌 حالة المشروع

**جاري العمل عليه** 🚧

---

## 🛠️ الإعداد والبيئة الافتراضية

تم إعداد المشروع بنجاح باستخدام بيئة بايثون الافتراضية لضمان استقلالية المكتبات.

| الخطوة | الأمر المنفذ | الحالة | الملاحظات/التحديات |
| :--- | :--- | :--- | :--- |
| **إنشاء البيئة** | `python -m venv venv` | ✅ تم | |
| **تفعيل البيئة** | `.\venv\Scripts\activate.bat` | ✅ تم | واجهنا خطأ أمان في PowerShell وتم التفعيل عبر `activate.bat` في Command Prompt. |
| **تثبيت المكتبات الأساسية** | `pip install SpeechRecognition PyAudio` | ✅ تم | تم تثبيت مكتبات الأذنين بنجاح. |
| **تثبيت مكتبة Gemini** | `pip install google-generativeai` | ✅ تم | |
| **تحديث المكتبة** | `pip install --upgrade google-generativeai` | ✅ تم | واجهنا مشكلة في الإصدار القديم وتم حلها بتحديث المكتبة. |

---

## 📂 المكونات المنجزة (حتى الآن)

لقد نجحنا في بناء واختبار المكونين الرئيسيين للمشروع بشكل منفصل: **الأذنين (الإدخال)** و**العقل (المعالجة)**.
![v](٢٠٢٥١١١١_١٨٠٤٤٣.jpg)

### 1. الأذنين (ears.py) - الإدخال الصوتي

| الملف | الوصف | حالة الاختبار |
| :--- | :--- | :--- |
| **`ears.py`** | يحتوي على دالة `listen_to_mic()` التي تستخدم مكتبتي `SpeechRecognition` و `PyAudio` للوصول إلى الميكروفون وتحويل الكلام المنطوق إلى نص (باستخدام محرك جوجل). | ✅ **ناجح** |

### 2. العقل (brain.py) - توليد الردود

| الملف | الوصف | حالة الاختبار |
| :--- | :--- | :--- |
| **`brain.py`** | يحتوي على دالة `get_gemini_response()` التي تستخدم Google Gemini API (`gemini-1.5-flash`) لتوليد ردود منطقية بناءً على مدخل نصي. | ⚠️ **مشكلة اتصال (Timeout)** |

---

## 🚨 التحديات والمشاكل الحالية (Next Steps)

المشكلة الحالية هي **فشل الاتصال** (Connection Timeout) مع خادم Gemini API، مما يمنع اختبار المكون الثاني.

| المشكلة | رمز الخطأ | الإجراءات المتخذة | الخطوة القادمة المقترحة |
| :--- | :--- | :--- | :--- |
| **فشل الاتصال** | `Timeout of 600.0s exceeded` (`503 failed to connect`) | تم التأكد من صحة مفتاح الـ API وتحديث المكتبة. | **فحص جدار الحماية (Firewall)** وإعدادات الشبكة، أو **التبديل الفوري لـ Cohere API** للتحقق مما إذا كان الخلل مقتصراً على Gemini. |

---

## 🎯 المرحلة التالية (Focus)

1.  **حل مشكلة الاتصال:** تنفيذ إحدى الخطوات المقترحة (فحص جدار الحماية أو التبديل لـ Cohere) لضمان الحصول على رد نصي ناجح.
2.  **بناء الصوت (`voice.py`):** تثبيت مكتبتي `gTTS` و `playsound` واختبار تحويل أي نص ثابت إلى كلام مسموع.
3.  **التجميع (`main.py`):** دمج الدوال الثلاث في ملف تنفيذي واحد لتشغيل البوت بالكامل.


